# Neon (or any Postgres) connection string for Prisma.
# In Neon: Dashboard → Connection details → copy the URI.
# For Vercel, the pooled connection string is preferred.
#
# Example:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
